// postcss.config.js (en la raíz del proyecto, junto a package.json)
module.exports = {
  plugins: {
    // Usar el plugin correcto para la nueva versión
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}